let arr = ["I", "go", "home"];
// delete arr[1];
// arr.shift();
console.log(arr);
delete arr[1]; // as array also an OBject it does not delte the key it delets the value
console.log(arr);
console.log(arr.length);
